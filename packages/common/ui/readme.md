Pure components shared between Meteor And MNW

Can perhaps sometimes make use of core logic / state

* Wallet specific components

The theme and style configuration

* Tamagui config
* New basic style components (buttons etc.)
